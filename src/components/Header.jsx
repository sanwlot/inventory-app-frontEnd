export default function Header() {
  const styles = {
    background: "black",
    color: "white",
    textAlign: "center",
  }
  return (
    <header style={styles}>
      <h1>Inventory App</h1>
    </header>
  )
}
